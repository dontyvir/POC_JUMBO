package cl.bbr.jumbocl.shared.hibernate;

//import org.apache.log4j.Logger;
//import org.hibernate.Session;
//import org.hibernate.SessionFactory;
//import org.hibernate.cfg.Configuration;

public class HibernateUtil {
	
/*	private static SessionFactory sessionFactory;
	
	static  {
		try {
			// Create the SessionFactory from hibernate.cfg.xml
			sessionFactory = new Configuration().configure().buildSessionFactory();
		} catch (Throwable ex) {
			// Make sure you log the exception, as it might be swallowed
			Logger logger = Logger.getLogger( "HibernateUtil" );
			logger.error("Error con creación de una sesión hibernate");
			throw new ExceptionInInitializerError(ex);
		}
	}

	public static SessionFactory getSessionFactory() {
		return sessionFactory;
	}
	
	public static Session sessionOpen() {
		Session sesfac = sessionFactory.openSession();
		return sesfac;
	}

	public static void sessionClose() {
		sessionFactory.close();
	}*/

}
